# Droidz Framework for Factory.ai v3.0

# User-specific directories (never commit!)
# .claude/  # Commented out - keeping template in repo for dual-mode support

# Unified specs directory (shared between CLI and Claude Code)
# Keep templates and examples, ignore active/archive work
.droidz/specs/active/*.md
.droidz/specs/active/*.json
!.droidz/specs/active/.gitkeep
.droidz/specs/archive/*.md
.droidz/specs/archive/*.json
!.droidz/specs/archive/.gitkeep

# Legacy: Factory-specific specs (deprecated in favor of unified .droidz/specs/)
.factory/specs/active/*.md
.factory/specs/active/*.json

# Validation cache (never commit)
.factory/validation/.validation-cache/

# User memory and active work
.factory/memory/user/
.factory/memory/org/*.json

# Generated validation (commit template, not instances)
# Note: .factory/commands/validate.md is auto-generated per project

# Configuration with API keys (NEVER commit!)
config.yml
!config.example.yml

# Dependencies
node_modules/
bun.lockb

# Backup and temporary files
*.backup.*
*-backup-*/
*-tasks.json
.droidz-install-*.log
README.md.v2-backup

# OS files
.DS_Store
Thumbs.db
