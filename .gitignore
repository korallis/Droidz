# Droidz worktrees
.runs/

# Configuration file with API keys (NEVER commit this!)
config.yml

# Keep the example template
!config.example.yml

# Claude Code - User-specific files only (but keep framework files)
.claude/memory/user/*.json
.claude/memory/org/*.json
.claude/.checkpoints/
.claude/commands/*.local.md

# Keep framework files in git
!.claude/agents/
!.claude/skills/
!.claude/standards/
!.claude/memory/org/README.md
!.claude/memory/user/README.md

# Dependencies are resolved via bun and should not be committed
node_modules/
bun.lockb

# Documentation files (keep only README.md and CHANGELOG.md in git)
HOW_TO_USE_DROIDZ.md
TOOL_COMPATIBILITY.md
SOLUTION_SUMMARY.md
V2.0.2_MIGRATION_GUIDE.md
