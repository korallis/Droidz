# Factory.ai Standards

- Edit files with ApplyPatch or Create tools, matching project style.
- Never skip validator commands captured in `.factory/commands/validate.md`.
- Keep instructions self-contained; no external doc links.
