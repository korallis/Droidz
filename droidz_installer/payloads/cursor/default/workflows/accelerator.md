# Cursor Accelerator Workflow

1. Load `.cursor/workflows/accelerator.md` in the command palette.
2. Gather repo context by reading `instructions/overview.md`.
3. Apply the Validation Gate: run ruff + pytest before summarizing.
